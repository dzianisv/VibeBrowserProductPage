# 2026-07-21

- Vibe Backlog inventory found two open documentation tasks: explain remote relay use
  on `/cli`, and direct remote MCP endpoint configuration on `/mcp`. Historical rows
  are already completed. Tracking/delivery issue: #157.
- Browser relay direct MCP endpoint is `https://relay.api.vibebrowser.app/mcp`; use
  `X-Remote-Session` with the extension UUID. It is UUID-only authentication, not the
  unrelated `mcp.vibebrowser.app` service and not attach-token authentication.
