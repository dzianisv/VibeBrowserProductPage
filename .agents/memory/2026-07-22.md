# 2026-07-22

- Remote MCP client configurations for task #160 were verified against official
  docs: Claude Code, Codex CLI, VS Code/GitHub Copilot, GitHub Copilot CLI,
  Cursor, and OpenCode each use distinct header/environment mechanisms. Use the
  task's agent-config evidence matrix before changing public configuration copy.
- Pi coding agent is the `earendil-works/pi` project (the prior `badlogic/pi-mono`
  location redirects there). It deliberately has no built-in MCP client; an MCP
  connection requires a custom or third-party extension, and there is no
  first-party adapter to recommend.
