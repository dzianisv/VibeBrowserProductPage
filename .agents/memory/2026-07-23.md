
## AgentPod Mobile product page — shipped live (session)
- Built + deployed AgentPod Mobile landing at https://agentpod.agentlabs.cc/mobile (also agentlabs.cc/mobile). Code in `apps/agentlabs` (agentlabs Vercel project, rootDir apps/agentlabs).
- PRs: #185 (page/routes) + #186 (assets). Both merged to main, deploy.yml `deploy-agentlabs` green.
- Routes: app/mobile (hero 10x video, features, how-it-works screenshots, providers, privacy, FAQ, forms), app/mobile/download (302→NEXT_PUBLIC_APK_URL||GH releases), app/api/waitlist (Brevo list 9 "AgentPod Mobile"), app/api/play-tester (adds to Google Group beta-testers@agentlabs.cc via DWD SA + Brevo, returns Play opt-in https://play.google.com/apps/internaltest/4701574809387172305).
- GOTCHA: assets `public/agentpod/*` were untracked → swept into `git stash -u` at session start; screenshots also hidden by `.git/info/exclude` `screenshots/` rule. First deploy 404'd media. Fixed in #186 by restoring videos from `stash@{0}^3` + `git add -f`. mp4 via LFS; Vercel needs GIT_LFS_ENABLED=1 (set on agentlabs project).
- DOMAIN cutover: agentpod.agentlabs.cc was owned by `www` project (prj_6VpB…, vibebrowser.app root) serving an old "AgentPod Fully Managed" page, and `openclaw.agentlabs.cc`(www) 308-redirected → agentpod. Moved agentpod → `agentlabs` project (prj_aupLF…); recreated openclaw→agentpod 308 redirect on agentlabs. DNS unchanged (Cloudflare CNAME agentpod→cname.vercel-dns.com). REVERT: delete both from agentlabs, re-add to www (agentpod plain, openclaw redirect→agentpod).
- Vercel agentlabs env (prod+preview): BREVO_API_KEY, BREVO_AGENTPOD_LIST_ID=9, GOOGLE_GROUPS_SA_JSON, GOOGLE_GROUPS_TARGET=beta-testers@agentlabs.cc, GIT_LFS_ENABLED=1. Mirrored to GH secrets too. SA=beta-enroll-sa@opencode-mobile-deploy.iam (DWD subject admin@agentlabs.cc, scope admin.directory.group.member).
- dzianisvv@gmail.com added to beta-testers@agentlabs.cc (initial tester, ACTIVE).
- OPEN DEPENDENCY: linking the Play Console internal-test track's tester list to the beta-testers@agentlabs.cc Google Group is a one-time Play Console UI step (browser-gated) — not done here.
