# 2026-08-06

- PR #217 proves `NextResponse.cookies.set` percent-encodes `vibe_attribution` JSON and Chrome preserves encoded octets. ProductPage now documents/tests this contract and bounds UTMs to relay limit 256. Extension companion fix remains required: `JSON.parse(decodeURIComponent(cookie.value))` in `captureInstallAttribution()`.
- Correction: PR #217 test executes NextResponse serialization and parses the Set-Cookie value; it does not execute Chrome. Extension PR #1843 is the actual reader fix. PR #217 only adds writer bounds and contract tests.
