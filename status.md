# Status — OAuth-first connector docs

```
ROADMAP  ████████████████████  7/7 done
 [x] 1. Audit stale "no OAuth" claims across pages
 [x] 2. Restructure connector guides OAuth-first (canonical URL + consent)
 [x] 3. Scopes explained (browser:read / browser:control) + revocation
 [x] 4. Legacy per-UUID kept as "Direct URL — headless/automation" + warning
 [x] 5. Fix /mcp, /mcp JSON-LD, /integrations hub; tests updated
 [x] 6. PR #226 CI green -> merged -> deployed -> IndexNow 200 (124 URLs)
 [x] 7. vibe-mcp submission pack §8.2 marked resolved (PR #138 merged)

WHY SLOW
 - not slow; single pass, CI green first try

NEXT
 - §8.1 remains: Claude Team/Enterprise plan needed to open the submission portal
```
